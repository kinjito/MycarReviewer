<!--$("#favorite-btn_< %= @post.id %>").html("< %= j(render 'favorites/favorite-btn', post: @post) %>");-->
#
$("#favorite-btn_<%= @post.id %> .favorite-btn").html("<%= escape_javascript(render partial: 'favorites/favorite-btn', locals: {post: @post}) %>");